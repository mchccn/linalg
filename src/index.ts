import { Matrix } from "./Matrix";
import { Vector2 } from "./Vector2";

const linalg = Object.freeze({ Matrix, Vector2 });

export { Matrix, Vector2 };
export default linalg;

module.exports = linalg;
exports.default = linalg;
